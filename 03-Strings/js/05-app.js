//reemplazar contenido del string
const producto = "monitor de 20 pulgadas";
console.log(producto.replace('pulgadas' , '"'))

//para recortar parte del contenido del String

console.log(producto.slice(0, 10));

console.log(producto.substring(9,2));

//substring sirve tambien como alternativa para cortar parte del contenido
